import React from "react";

// components
import WrapperItem from "./WrapperItem";

interface Props {}

const BoxDebtorBase: React.FC<Props> = (props) => {
    return <WrapperItem />;
};

export default BoxDebtorBase;
